/// A pattern dot may have a string value (a single character in thought) that will be presented visually to the user.
class PatternDot {
  /// The string value (a single character in thought)
  final String value;
  const PatternDot({required this.value});
}
